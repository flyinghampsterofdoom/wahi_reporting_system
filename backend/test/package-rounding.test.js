'use strict';
const {test}=require('node:test'),assert=require('node:assert/strict');
const {packageQuantity,decimal,build}=require('../import/importer');
test('approved package rounding normalizes only the observed 33.8 artifact',()=>{assert.equal(packageQuantity('33.799999999999997'),'33.8');for(const v of ['0.00113636','29.5735295625','33.799','33.8123456789'])assert.equal(packageQuantity(v),v);assert.equal(decimal('33.799999999999997'),'33.799999999999997');});
test('package rounding preserves raw workbook evidence and normalizes package label and contents',async()=>{const source=structuredClone(require('./fixtures/drop-source.json')),sheet=source.sheets.find(s=>s.name==='Ingredients'),row=sheet.rows[1];row.cells['F'+row.row].value='33.799999999999997';const r=await build(source,{activationAt:'2026-09-18T00:00:00.000Z'});assert.equal(r.state.purchaseOptions[0].contentQuantity,'33.8');assert.equal(r.state.purchaseOptions[0].label,'33.8 oz');const evidence=r.state.importRecords.find(r=>r.sheet==='Ingredients');assert.equal(evidence.sourceValues.values.Size,'33.799999999999997');assert.ok(evidence.issues.some(i=>i.concept==='Deterministic quantity normalization'));});
